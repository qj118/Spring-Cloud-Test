server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        # server-addr: localhost:8848
        server-addr: 192.168.110.122:1111 # nginx + nacos 集群配置

management:
  endpoints:
    web:
      exposure:
        include: '*'
