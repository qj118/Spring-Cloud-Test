server:
  port: 3366

spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      name: config
      profile: dev
      uri: http://localhost:3344

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:7001/eureka

rabbitmq:
  host: localhost
  port: 15672
  username: guest
  passord: guest

management:
  endpoints:
    web:
      exposure:
        include: "*"